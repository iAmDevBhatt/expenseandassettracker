# Copy this file to .env and update values for your environment

# Database — SQLite (default, no setup needed):
DATABASE_URL=sqlite:///./data/tracker.db

# Database — PostgreSQL (uncomment to use):
# DATABASE_URL=postgresql://tracker_user:tracker_pass@localhost:5432/trackerdb

# JWT — CHANGE THIS in production (generate with: python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b

JWT_EXPIRY_HOURS=24

DEBUG=false
